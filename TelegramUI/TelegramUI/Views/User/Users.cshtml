@using TelegramUI.Models
@model UserViewModel
@{
    ViewData["Title"] = "Users";
}

<h2>Users</h2>

<table class="table">
    @foreach (Users p in Model.user)
    {
        <tr><td>@p.id1</td></tr>
    }
</table>